var sContent = txtFile.ReadToEnd();
                    var CultureInfo = new CultureInfo("en-US");

                    using (CsvReader csv = new CsvReader(new MemoryStream(UTF8Encoding.UTF8.GetBytes(sContent)), false, Encoding.UTF8))
                    {
                        csv.SupportsMultiline = false;
                        csv.MissingFieldAction = MissingFieldAction.ReplaceByEmpty;
                        csv.SkipEmptyLines = true;
                        
                        var records = csv.GetEnumerator();
                        while (records.MoveNext())
                        {
                            var vals = records.Current;
                            try
                            {
                    
